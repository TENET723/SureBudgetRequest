@page "/admin/currencies"
@inject IMediator Mediator
@inject ICurrentUser CurrentUser
@inject NavigationManager Nav

@using SureBudgetRequest.Application.Currencies.Queries
@using SureBudgetRequest.Application.Currencies.Commands.CreateCurrency
@using SureBudgetRequest.Application.Currencies.Commands.UpdateCurrencyRate

<PageTitle>Currencies</PageTitle>

<div class="container-fluid py-3">
    <div class="d-flex align-items-center justify-content-between mb-3">
        <h5 class="mb-0">Currencies</h5>
        <button class="btn btn-sm btn-primary" @onclick="OpenCreate">+ New Currency</button>
    </div>

    @if (!string.IsNullOrEmpty(_errorMessage))
    {
        <div class="alert alert-danger alert-dismissible">@_errorMessage <button type="button" class="btn-close" @onclick="() => _errorMessage = null"></button></div>
    }
    @if (!string.IsNullOrEmpty(_successMessage))
    {
        <div class="alert alert-success alert-dismissible">@_successMessage <button type="button" class="btn-close" @onclick="() => _successMessage = null"></button></div>
    }

    <div class="form-check mb-2">
        <input class="form-check-input" type="checkbox" id="showInactiveCurrencies" @bind="_showInactive" @bind:after="LoadData" />
        <label class="form-check-label small" for="showInactiveCurrencies">Show inactive</label>
    </div>

    @if (_loading)
    {
        <div class="text-center py-4"><div class="spinner-border spinner-border-sm"></div></div>
    }
    else
    {
        <div class="table-responsive">
            <table class="table table-sm table-hover align-middle">
                <thead class="table-light">
                    <tr>
                        <th>Code</th>
                        <th>Name</th>
                        <th class="text-end">Rate to MMK</th>
                        <th>Last Updated</th>
                        <th>Status</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var c in _currencies)
                    {
                        <tr class="@(!c.IsActive ? "text-muted" : "")">
                            <td><strong>@c.Code</strong></td>
                            <td>@c.Name</td>
                            <td class="text-end">@c.RateToMmk.ToString("N4")</td>
                            <td><span class="small text-muted">@c.RateUpdatedAt.ToLocalTime().ToString("yyyy-MM-dd HH:mm")</span></td>
                            <td>
                                @if (c.IsActive) { <span class="badge bg-success">Active</span> }
                                else             { <span class="badge bg-secondary">Inactive</span> }
                            </td>
                            <td class="text-end">
                                <button class="btn btn-xs btn-outline-secondary btn-sm" @onclick="() => OpenEdit(c)">Edit</button>
                                <button class="btn btn-xs btn-outline-secondary btn-sm ms-1" @onclick="() => OpenHistory(c.Code)">History</button>
                            </td>
                        </tr>
                    }
                    @if (!_currencies.Any())
                    {
                        <tr><td colspan="6" class="text-center text-muted py-3">No currencies found.</td></tr>
                    }
                </tbody>
            </table>
        </div>
    }
</div>

@* ── Create / Edit Modal ────────────────────────────────────────────────────── *@
@if (_showForm)
{
    <div class="modal d-block" tabindex="-1" style="background:rgba(0,0,0,.4)">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h6 class="modal-title">@(_editingCode is null ? "New Currency" : $"Edit Currency — {_editingCode}")</h6>
                    <button class="btn-close" @onclick="CloseForm"></button>
                </div>
                <div class="modal-body">
                    @if (!string.IsNullOrEmpty(_formError))
                    {
                        <div class="alert alert-danger small py-2">@_formError</div>
                    }

                    <div class="mb-3">
                        <label class="form-label small fw-semibold">Code</label>
                        <input class="form-control form-control-sm"
                               @bind="_formCode"
                               placeholder="e.g. USD"
                               maxlength="10"
                               disabled="@(_editingCode is not null)" />
                        <div class="form-text small">3-letter ISO code is conventional. Cannot be changed after creation.</div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label small fw-semibold">Name</label>
                        <input class="form-control form-control-sm" @bind="_formName" placeholder="e.g. US Dollar" />
                    </div>

                    <div class="mb-3">
                        <label class="form-label small fw-semibold">Rate to MMK</label>
                        <input class="form-control form-control-sm"
                               type="number"
                               step="0.000001"
                               min="0"
                               @bind="_formRate"
                               disabled="@(_editingCode == "MMK")" />
                        <div class="form-text small">
                            1 @(_formCode?.ToUpperInvariant() ?? "unit") = @(_formRate.ToString("N4")) MMK.
                            @if (_editingCode is not null && _originalRate.HasValue && _originalRate.Value != _formRate)
                            {
                                <span class="text-warning">Rate change will be logged.</span>
                            }
                        </div>
                    </div>

                    @if (_editingCode is not null && _editingCode != "MMK")
                    {
                        <div class="form-check">
                            <input class="form-check-input" type="checkbox" id="formActive" @bind="_formIsActive" />
                            <label class="form-check-label small" for="formActive">Active</label>
                        </div>
                    }
                </div>
                <div class="modal-footer">
                    <button class="btn btn-sm btn-secondary" @onclick="CloseForm">Cancel</button>
                    <button class="btn btn-sm btn-primary" @onclick="SaveForm" disabled="@_saving">
                        @if (_saving) { <span class="spinner-border spinner-border-sm me-1"></span> }
                        Save
                    </button>
                </div>
            </div>
        </div>
    </div>
}

@* ── Rate History Modal ─────────────────────────────────────────────────────── *@
@if (_historyCode is not null)
{
    <div class="modal d-block" tabindex="-1" style="background:rgba(0,0,0,.4)">
        <div class="modal-dialog modal-dialog-centered modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h6 class="modal-title">Rate History — @_historyCode</h6>
                    <button class="btn-close" @onclick="() => _historyCode = null"></button>
                </div>
                <div class="modal-body">
                    @if (_historyLoading)
                    {
                        <div class="text-center py-3"><div class="spinner-border spinner-border-sm"></div></div>
                    }
                    else if (!_history.Any())
                    {
                        <p class="text-muted small mb-0">No rate changes recorded.</p>
                    }
                    else
                    {
                        <table class="table table-sm">
                            <thead>
                                <tr>
                                    <th>When</th>
                                    <th class="text-end">Old Rate</th>
                                    <th class="text-end">New Rate</th>
                                    <th class="text-end">Change</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var h in _history)
                                {
                                    var delta = h.NewRate - h.OldRate;
                                    <tr>
                                        <td class="small">@h.ChangedAt.ToLocalTime().ToString("yyyy-MM-dd HH:mm")</td>
                                        <td class="text-end">@h.OldRate.ToString("N4")</td>
                                        <td class="text-end">@h.NewRate.ToString("N4")</td>
                                        <td class="text-end @(delta >= 0 ? "text-success" : "text-danger")">
                                            @(delta >= 0 ? "+" : "")@delta.ToString("N4")
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    }
                </div>
            </div>
        </div>
    </div>
}

@code {
    private List<CurrencyDto> _currencies = new();
    private bool _loading = true;
    private bool _showInactive;
    private string? _errorMessage;
    private string? _successMessage;

    // Create/Edit state
    private bool _showForm;
    private bool _saving;
    private string? _formError;
    private string? _editingCode;       // null = create mode
    private decimal? _originalRate;     // captured on edit, used to flag a rate change
    private string _formCode = "";
    private string _formName = "";
    private decimal _formRate;
    private bool _formIsActive = true;

    // History state
    private string? _historyCode;
    private bool _historyLoading;
    private List<CurrencyRateChangeDto> _history = new();

    protected override async Task OnInitializedAsync()
    {
        if (CurrentUser.Role != UserRole.Admin)
        {
            Nav.NavigateTo("/", replace: true);
            return;
        }
        await LoadData();
    }

    private async Task LoadData()
    {
        _loading = true;
        var result = await Mediator.Send(new ListCurrenciesQuery(IncludeInactive: _showInactive));
        if (result.IsSuccess)
            _currencies = result.Value.OrderBy(c => c.Code).ToList();
        _loading = false;
    }

    private void OpenCreate()
    {
        _editingCode = null;
        _originalRate = null;
        _formCode = "";
        _formName = "";
        _formRate = 1;
        _formIsActive = true;
        _formError = null;
        _showForm = true;
    }

    private void OpenEdit(CurrencyDto c)
    {
        _editingCode = c.Code;
        _originalRate = c.RateToMmk;
        _formCode = c.Code;
        _formName = c.Name;
        _formRate = c.RateToMmk;
        _formIsActive = c.IsActive;
        _formError = null;
        _showForm = true;
    }

    private void CloseForm()
    {
        _showForm = false;
        _formError = null;
    }

    private async Task SaveForm()
    {
        _formError = null;

        if (string.IsNullOrWhiteSpace(_formCode)) { _formError = "Code is required."; return; }
        if (string.IsNullOrWhiteSpace(_formName)) { _formError = "Name is required."; return; }
        if (_formRate <= 0) { _formError = "Rate must be greater than zero."; return; }

        _saving = true;
        try
        {
            if (_editingCode is null)
            {
                var result = await Mediator.Send(new CreateCurrencyCommand(_formCode, _formName, _formRate));
                if (result.IsFailure) { _formError = result.Error.Message; return; }
                _successMessage = $"Currency '{result.Value}' created.";
            }
            else
            {
                var result = await Mediator.Send(new UpdateCurrencyCommand(
                    _editingCode, _formName, _formRate, _formIsActive, CurrentUser.UserId));
                if (result.IsFailure) { _formError = result.Error.Message; return; }
                _successMessage = $"Currency '{_editingCode}' updated.";
            }

            _showForm = false;
            await LoadData();
        }
        finally { _saving = false; }
    }

    private async Task OpenHistory(string code)
    {
        _historyCode = code;
        _historyLoading = true;
        _history = new();
        StateHasChanged();

        var result = await Mediator.Send(new ListCurrencyRateChangesQuery(CurrencyCode: code, Take: 100));
        if (result.IsSuccess)
            _history = result.Value.ToList();

        _historyLoading = false;
    }
}
